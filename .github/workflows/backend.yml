name: Deploy to Render

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger a Render
      run: |
        curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
